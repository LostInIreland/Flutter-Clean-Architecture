/// Representation for a [ArticleView] instance for this layers Model representation
class ArticleView {
  int id;
  String title;
  String type;
  int descendants;

  ArticleView(this.id, this.title, this.type, this.descendants);
}