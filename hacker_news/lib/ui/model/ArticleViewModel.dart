/// Representation for a [ArticleViewModel] fetched from an external layer data source
class ArticleViewModel {
  String title;
  String type;
  String descendants;

  ArticleViewModel(this.title, this.type, this.descendants);
}